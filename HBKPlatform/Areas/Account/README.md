Account Area
============

Note: This uses the ASP.Net Razor Pages framework instead of plain MVC.

To find the model that is used to display/update each view, look for the .cshtml.cs files paired with each page.